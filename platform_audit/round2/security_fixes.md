# Round 2 security/technical fixes (layout/App/functions agent)

- Task 1 DONE: added caller check (signed-in non-admin -> 403; scheduler with no user still runs) to sendWeeklySummaryNotifications, sendWeeklyVoiceNote (both already de-dupe per week). Added staff-only 403 (super_admin/admin/school_admin/teacher) to charterHealthMetrics, exportCurriculumOutline, exportPolicyToPDF, exportPolicyToWord (+ school_id scope for non-admins; master templates allowed), generateForestSchoolImage, getActiveCharter, validateAudioAgainstCharter, validateSpecAgainstCharter. Left as-is (parent features, login required; role=user exclusion is a Yasmin decision): askFounder, generateChapterAudio, generateBookChapterAudio, generateMilestoneImage, generateSongPracticeSheet, readScriptAloud, exportJourneyChapter, sendReEntryPush, parentRecommendations. bookTrainingSession public by design; musicGenerationCallback is a provider webhook (bearer for non-Suno, taskId match for Suno) - unchanged.
- Task 2 DONE (R-12): Sidebar.jsx - removed from school_admin nav: /schools, /users, /kasvunpolku/admin, /network, /curriculum-canvas, /bulk-generation, /admin/music-room (+ empty "Admin Studio" header); from teacher nav: /kasvunpolku/admin, /admin/music-room. Added canRoleOpenPath() export in RoleRouteGuard.jsx (guard logic unchanged) and a Sidebar safety filter so no link is shown that the guard would bounce. role missing/"user" now gets a Dashboard-only nav (was teacherNav) - matches the guard (R-14/R-15 nav part). ReadOnlyGuard.jsx: removed unused React import (lint).
