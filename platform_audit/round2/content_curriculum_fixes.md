# Round 2 — content & curriculum code fixes

- Verified earlier work (git diff 0fe568d..HEAD): T-15 PedagogyTipCard, T-16 FestivalBanner, T-17 SongReviewCTACard, T-18..T-21 LaunchReadinessCard, T-22/T-23 SuperAdminDashboard + useSuperAdminStats, GrowingBooksCard sample text, season words in CanvasGeneratePanel + BulkCurriculumGeneration — complete.
- Status check (already fixed in round 1, CSV not updated): C-08 (FinnipedWeekPanel error/no-file states), C-09 (DailyPlan week fallback), C-10 (DailyPlan hides merged tiers), C-13/T-26 (ActivityLibrary sample data removed), C-17 UI parts (ForestSchool "All Year", DailyMomentCard/useDailyMomentPhoto, ParentThisMonth), T-07 ("Audio coming soon" gone).
- C-11 (code part) fixed: src/pages/curriculum/CurriculumHome.jsx now prefers status "canonical" over merged same-name tiers (was: whichever had heartwise_framework), and shows LoadError on a failed AgeGroup load instead of spinning. Evening tier status/merged_into is a DATA decision (left).
- L-20 fixed: src/components/finniped/LibraryByWeek.jsx rebuilt. Groups books by Term N · Week k from published FinnipedPlacement (month + week_in_month + band → resource_row_id, page range) plus the old linked_week_ids (via non-archived master CurriculumWeek → CurriculumMonth). Published books only; gated books and gated placements hidden from non-staff; respects the library band filter; April/August/December weeks show the consolidation line; "View" opens the reader at the week's first page ("This week: pages a–b" caption). FinnipedLibrary passes band/isStaff and page={viewing._page} to PdfViewerDialog.
- L-16 fixed: FinnipedLibrary visible filter hides necessity=unnecessary && necessity_approved from non-super-admins.
- L-14 fixed: My Resources shelf applies the same published/gated checks; missing, unpublished-without-successor or gated favourites show "This book is no longer available." with an un-save star, not a stale link.
- C-08 remainder fixed: FinnipedWeekPanel no longer lists all 5000 FinnipedResource rows per week; it .get()s only the 1–4 books the week's placements point at (published, gating respected), and flags a load error if any fetch fails.
